---
name: docx-report
user-invocable: true
description: 以既有 Word（.docx）模板为骨架、结合工作区调研内容生成正式中文调研报告；完整还原模板样式，产出无自动编号、带可点击上标引用角标与详尽参考文献的 .docx。
---

# Docx Report - 模板化调研报告生成

## 触发方式

`/docx-report <模板.docx 路径> [输出目录]`

例：`/docx-report /root/Working/Macao/Output_Report/Report_Template/xxx.docx /root/Working/Macao/Output_Report`

## 适用场景

- 用户给出一份 **.docx 提纲/模板**，要求"结合 space 中的所有调研内容"生成一份**正式报告**。
- 需要交付 **Word 成品**（而非 Markdown），且对版式、引用、参考文献有明确要求。
- 工作区里已有大量一手/二手调研资料（`Interview_Research/`、`company-dossier/`、`*.md`、`*.csv`、PDF、截图），需要被系统性地"用起来"而不是重新调研。

不适用：纯 Markdown 报告、无模板的自由写作、单纯的资料汇总。

## 输入契约（开工前必须逐一盘点）

| 输入 | 典型位置 | 地位 |
|---|---|---|
| **模板 .docx** | 用户指定 | R1 的骨架，章节结构以此为准 |
| **在线问卷原始回复** | `<poll>/result/<poll>/responses.csv` + **`survey_definition.json`** | **一线一手数据，必须用**；详见 R9 |
| 企业尽调底稿 | `Interview_Research/<专题>/…调研报告.md` + `sources/` | 结构化结果 + 逐字摘录 |
| 结构化结果 / 台账 | `results/*.json`、判给台账、`SOURCES.md` | 数字与文号的可回溯出处 |
| 政策原文 / 公开资料 | 各专题 `sources/`（网页、PDF、截图） | `official` 级证据 |

> ⚠️ **在线问卷是带"必答约束"的仪器，不是舆情抽样。** 读 `survey_definition.json` 里
> `required: true` 的题目（如 q4 当前在用算力、q5 来源、q8 训练/推理分配、q11–q13 需求、
> q26 机柜现状）：**完整提交中必然有答案**。因此这些字段不得写成"无回复/未公开"。
> 用 `assets/parse_survey.py` 先把问卷落成《当前算力台账》，再动笔写现状章。

## DSH 工具映射

| 用途 | 工具 |
|---|---|
| 读模板 / 源文件 | `bash`（unzip 解析 docx）+ `read` |
| 盘点工作区 | `glob` / `grep` / `read` |
| 缺口补研 | `skill(research)` 模式 B，或直接 `subagent` + `web_search` |
| 并行专题底稿 | `subagent`（后台，每专题 1 个） |
| 落盘 | `write` / `edit` |
| 生成 docx | `bash` + `python3 assets/md2docx.py` |
| 交付 | `present` |

---

## 一、硬性要求（用户验收标准，必须逐条满足）

> 以下 R1–R10 是本 skill 的**验收口径**，不是建议。
> R1–R5 来自用户的明确反馈；**R6.1–R6.4** 借鉴《澳门科技企业AI价值场景调研COT-9.0-澳门科技特化版》的"物理确定性 / 调研量纲 / 专家基准 / 战略对齐偏离"四条准则（该文件为输入侧 COT，只取其数据可信度契约，不取其调研流水线与领域内容）；**R8** 取其"矛盾—场景映射"的筛选逻辑。

### R1 模板保真
先用 `unzip` 解开模板 `.docx`，从 `word/document.xml`、`word/styles.xml` 提取**样式基线**，逐项还原：

- `w:docDefaults`：默认字体、字号（`w:sz` 为半磅，22 = 11pt）、行距（`w:line` 288 = 1.2 倍）；
- 各级标题：字体（如 `等线`/`Arial`）、字号（章 32=16pt、节 24=12pt）、是否加粗、`outlineLvl`、段前段后间距；
- 页面：`w:pgSz`（A4 = 11905×16840 twips）、`w:pgMar` 页边距；
- 章节结构：**逐节对应模板提纲**，不得缺章、不得擅自改章序。

详见 `TEMPLATE_SPEC.md`。

### R2 禁止 Word 自动编号（最高优先级）
**绝对不要对列表使用 `List Number` / `List Bullet` 样式或写入 `<w:numPr>`。**
Word 会把全文档所有引用同一编号定义的段落视为**同一个序列**，导致打开后出现"整篇文档跨标题连续编号"的严重版式错误。

正确做法：列表一律渲染为**普通段落 + 字面量标记**：
- 有序：`1.` `2.` `3.` 作为文本写入，**每个列表独立从 1 重新计数**；
- 无序：`•` 作为文本写入；
- 用 `left_indent` + 负 `first_line_indent` 制造悬挂缩进。

### R3 引用角标必须可点击跳转
- 正文引用以上标形式呈现：`vertAlign=superscript`、正文字号的 ~0.8 倍、深蓝色（如 `#1F497D`）；
- 每个角标是 **内部超链接**（`<w:hyperlink w:anchor="_RefN">`），点击跳转到《参考文献》对应条目；
- **多编号引用必须逐个成链**：`[[1,2,5,20,42]]` 要渲染成 `[` + 链接1 + `,` + 链接2 + … + `]`，
  **每个数字各自是一条超链接**（分别指向 `_Ref1`、`_Ref2`、`_Ref5`…），**不得把整串做成一个指向首编号的链接**；
- 参考文献条目段落必须有对应书签 `<w:bookmarkStart w:name="_RefN">`，书签 id 全文档唯一；
- 交付前必须校验**所有 anchor 均有同名书签**（零失效链接），且**链接总数 ≈ 各引用编号个数之和**（而非角标段落数）。

### R4 参考文献必须详尽
每条参考文献至少包含：**机构/作者 + 文件名/标题 + 文号或日期 +（如有）URL 或工作区落盘路径**。
- 按主题分组编号（国家战略 / 地方政策 / 法律法规 / 统计基础设施 / 企业与行业 / 本次调研既有成果 / 补充调研专题…）；
- **分组标签必须是标题（`#### 【一】…`），不是加粗正文**——否则不会出现在 Word 导航窗格的标题树里；
- 条目在 Markdown 中写作 `Rn. <内容>`，由转换器自动生成书签并渲染为 `[n] <内容>`；
- 附录末尾附一句"原始网页/PDF 与逐字摘录落盘位置"，保证可回溯。

### R5 重要判断、结论与表格数据都必须有引用
- 覆盖范围：**核心结论摘要、定位判断、各章小结、方案对比结论、模式价值、所有含数字/金额/法规编号的段落**；
- **表格同样必须挂角标**：关键数据速览表、统计表、对标表、判给台账、预测表的每一行数据，只要能定位到源，都要在**该行最后一个单元格（通常是"口径与来源/说明/依据/备注"列）末尾**追加 `[[n]]`；同一格多源写作 `[[a,b]]`；
  - 转换器已支持表格内引用（`add_table(..., allow_cite=True, cite_size=8.0)`），角标按表内字号自动降级为 8pt；
  - 多行规则用脚本批量追加，不要手改（见 `assets/add_table_citations.py`）；
  - **推荐做法**：在"关键数据速览"表下加一句表注——"本表及各章表格中的上标数字为参考文献编号，点击可跳转至附录F对应条目"；
- **不允许存在"僵尸文献"**：参考文献表里的每一条都必须在正文或表格中被至少引用一次；交付前统计被引用编号集合，若 1..N 有缺口，要么补挂角标、要么删除该条目；
- 密度：达不到"每段都有"时，至少保证**所有结论性段落 100% 有角标**；
- 交付前用脚本统计"含结论/判断/建议/定位 等关键词的段落中有角标的比例"，必须为 **100%**。

### R6 证据纪律
- 分级：`official`（政府/公报/监管文件）＞ `company`（企业自述）＞ `media`（第三方媒体）＞ `inference`（推算）；
- **不编造**：拿不到就写"未取得/未公开（负面发现）"——负面发现与正面事实同等有价值；
- **口径矛盾并录**：两个官方口径不一致时并列呈现并注明差异，不得静默择一；
- 推算一律就地标注 `【推算】`，不与已核实事实混同。

> R6.1–R6.4 为**数据可信度契约**，借鉴自《澳门科技企业AI价值场景调研COT-9.0》的"量纲—公式—基准"体系：它解决的是"这个数字凭什么可信、别人能不能复算"。

#### R6.1 推算三件套（强制）
标 `【推算】` 只是起点，不是终点。任何推算必须同时给出：
1. **公式**——写在正文可见处（纯文本即可，如 `P ≈ QPS × T_total × M / η`），**不得只把公式留在底稿或子代理报告里**；
2. **参数取值**——每个入参的值 + 口径 + 来源角标（如 `QPS = 2（政府 Helpdesk 0.5–2 QPS/千用户 [[n]]）`）；
3. **基准/系数来源**——专家基准或行业默认值必须注明出处（如 `η = 0.6，来源 [[n]]`）。

> 唯一检验口径：**读者拿着报告能否自己复算出同一个数**。复算不出，就是三件套缺件。
> 系数与基准参数集中放《附录·调研方法与口径说明》（我方报告里的 A100 等效折算系数 `H800≈2.0×A100 / V100≈0.25×A100 / 寒武纪≈0.3×A100` 即此类），正文只引用不重复。

#### R6.2 量纲可复核（每个数字都要能回答"谁、被问了什么"）
正文每个关键数字必须可回答三问：
1. **量纲/单位**（A100 等效卡时/年、PFLOPS(FP16)、MOP/月…）；
2. **采集通道**（文件名 / 判给台账编号 / 问卷题号 / 访谈岗位）——推荐直接写成可复核句式：`请询问 <岗位>："<问题1>？<问题2>？"`；
3. **口径与年份**。

> 交付前抽查：任取 **10 个数字**逐个在 `sources/`、台账或问卷中定位原始出处；定位不到的，降级表述或改标 `【估算】`。

#### R6.3 量级护栏
- 每类推算给出**量级上下限**；越界必须**解释原因或拆分为子项**，不得直接给数；
- **禁止把"算术和"当"瞬时负载"**：典型坑是把 N 个场景/单位的峰值相加得出总需求（会系统性高估）。并集/峰值类数字必须显式注明"**非任一时点负载**"，并说明其中多少是客户侧/非自有资产。

#### R6.4 叙事—证据对照（自述必须交叉验证）
对任何主体（企业/机构/部门）的**自述能力**，必须列出三栏并给出判定：
- **支持证据**：可验证的合同、判给台账、收入贡献、专利/软著、在册认证；
- **不支持证据**：可核验的反证（如"10 个在招职位中 0 个 AI/算法岗""官网无该产品线的任何页面"）；
- **偏离判定**：叙事与证据是否匹配；偏离时必须写出结论句（如"AI 叙事强于 AI 编制"）。

> 禁止只转述自述：`company` 级证据**不得单独支撑结论性判断**，必须与 `official`/`media`/台账交叉后方可下结论。

### R7 语言与命名
- 正文统一**简体中文**；引用来源若为繁体，转写为简体；
- **公司/机构名保留其登记的原文用字**，避免识别歧义；
- 文件名建议 `<报告全名><YYYYMMDD>.docx`，与模板同目录或用户指定目录。

### R8 问题—建议可追溯（建议不得悬空）
每条建议/方案必须显式锚回它要解决的**现状问题**，并用代价论证优先级：
- 建议段内回指问题所在章节（如"→ 见 3.4.1"）并挂该问题的引用角标；
- 必须给出**不解决的量化代价**（如"投标方案人工产出 X 人天/单 × 年 Y 单 = 浪费 Z 人年"），不得只用形容词；
- **交付物**：附录增一张 **「短板/问题 → 建议 → 量化代价 → 依据」映射表**，逐行挂角标。

> 这张表是"建议不悬空"与 R5 引用覆盖率的共同验收物；也是决策型报告最容易被追问的一张表。

### R9 问卷答案必须落进现状（"当前算力"不得记为"无回复"）
**根因教训：把已作答的必答题当成缺答，会系统性低估现状。** 澳门项目上一版报告即因
"只填了高校装机 + 把企业侧写成'未公开披露'"，把当前算力少算了一半以上。

1. **必答题必然有答案**：`survey_definition.json` 中 `required=true` 的字段，在
   `finished=True` 的完整提交里 **100% 有值**。先算"完整提交中该题的作答率"——
   若为 100%，就不允许在报告中写"无回复/未公开"。
2. **`0` 是有效答案**（＝无在用算力），**不是缺答**；`0` 与"空"必须分开统计与表述。
3. **"当前算力"必须从 q4 填充**，并按**两个口径分列，绝不混算**：
   - **口径A 本地装机**（自建／私有化部署，物理在澳）——由 q5「算力来源」判定；
   - **口径B 在用算力**（含租赁公有云）——q4 的原始含义。
   q5＝"以上都有"的混合型企业需说明"本地/云无法从本问卷拆分"，给出**下界**（纯自建之和）
   与**上界**（自建＋混合），不得取单一均值掩盖区间。
4. **q4 必须与 q6 交叉校验后才可采信或否定**：把 q6_detail 的"卡型×数量"按单卡/整机
   FP16 基准折成 P，与 q4 比对。
   - 比值落在 0.5—2.0× → **判为自洽，直接采用**；
   - 比值越界或卡型未识别 → 分条标 `【冲突】`/`【存疑】`，说明分歧点；
   - **禁止在未做逐条校验的情况下，把整批自报值判为"单位存疑"或"按卡数理解"**（这会
     误杀可信数据）。
5. **"当前算力"总口径必须分项拼装**，至少含：
   `企业（问卷 q4）＋ 高校与科研 ＋ 政务/社会资本（有公开者）`。
   **高校侧不在企业问卷内，必须单独并入**；未公开的分项记为负面发现，不得静默省略。
   每个分项标注**口径（本地装机/在用）、年份、折算基准**。
6. 硬件换算基准（单卡/整机 FP16 稠密算力）与 A100 等效折算系数，一并写进
   《附录·调研方法与口径说明》并注明来源（R6.1）。

### R10 算力指标必须逐值精确引用
适用范围：**任何算力指标数值**——`PFLOPS` / `P@FP16` / `Eops` / `EFLOPS` / `A100 等效` /
`卡时` / `加速卡张数` / `GPU 数` / `上架率·利用率` / `算力规模`（含"元/卡时"类单价）。

1. **贴到值上（精确到值，不是精确到行）**：角标必须紧贴该数值——同一段落内紧随数值之后，
   或**该数值所在的表格单元格之内**。
   **不得**只把角标挂在整行的"来源/备注"列、段末或章节末；那是"粗到行"的引用，
   读者无法判断这个数究竟出自哪一条。
2. **引到条（唯一定位"哪份文件的哪一处"）**：

   | 来源类型 | 条目必须写到 |
   |---|---|
   | 政府文件 / 法规 / 判给 | 机构 + 文件名 + **文号或批准文件号** + 日期 + 具体定位（第 X 条 / 表 X / 第 Y 页） |
   | 网页 / 新闻 | 标题 + 机构 + 发布日期 + **URL** + 抓取日期 |
   | 企业财报 / 招股书 | 报告名 + 报告期 + **页码或章节** |
   | 一手调研底稿 | 文件路径 + **记录级定位**（记录号 / 企业名 / 题号），如"问卷记录 #19，q4" |
   | 高校 / 平台公开资料 | 单位 + 资料名 + **具体规格表** + 抓取日期 |

3. **推算引输入**：标 `【推算】` 的算力指标，除公式与参数（R6.1）外，必须**逐个入参引用其来源**；
   不得只引"本报告推算"。
4. **禁止组引用兜底**：不得用 `[[74,75,77]]` 这类"调研报告整体"条目去支撑一个**具体算力数字**；
   若该数字确系该报告的原创推算，按第 3 条处理（给公式＋参数来源）。
5. **口径随值**：同一处出现两个口径不一的算力值（如 `4 Eops` 与 `1.16 EFLOPS`），
   必须**各自引用并注明口径**，不得合并成一个角标。

> 可机器校验的部分（Step 7）：**数值单元格的"自带角标率"必须为 100%**。
> 上一版澳门报告实测仅 **34%**（122 个数值单元格里 41 个自带角标，其余 66% 粗到行）——
> 这正是 R10 要消灭的形态。

---

## 二、执行流程

### Step 0：解析模板
```bash
cd <模板目录> && python3 - <<'PY'
import zipfile, re
z = zipfile.ZipFile('<模板>.docx')
d = z.read('word/document.xml').decode('utf-8')
paras = re.findall(r'<w:p[ >].*?</w:p>', d, re.S)
for i, p in enumerate(paras):
    t = ''.join(re.findall(r'<w:t[^>]*>(.*?)</w:t>', p, re.S))
    style = re.search(r'w:pStyle w:val="([^"]+)"', p)
    print(i, style.group(1) if style else '', t)
PY
```
同时读取 `word/styles.xml` 的 `<w:docDefaults>` 与 `document.xml` 末尾的 `<w:sectPr>`，记录字体/字号/行距/页面。

**产出**：一份"模板章节清单 + 样式基线表"，作为后续写作与转换的对照基准。

### Step 1：盘点工作区
用 `glob`/`grep` 建立"来源清单"：
- 用户点名的参考资料（务必全部读完并提取要点）；
- 同目录/兄弟目录的既有调研报告、结构化结果（`results/*.json`）、落盘一手资料（`sources/`）、问卷 `*.csv`、截图 `*.png`；
- 关键词检索补漏（政策名、机构名、金额、指标）。
   
**产出**：`章节 → 可用来源` 的映射表，并标出**缺口**。

> 若工作区有在线问卷，**先跑** `python3 assets/parse_survey.py --csv <responses.csv> --def <survey_definition.json> --out <输出目录>/survey_current_compute.md`，把现状章的"当前算力"一次填对（R9），再谈写作。

### Step 2：缺口补研（三选一，按优先级）
1. **先查其他 source 文件**——多数"缺失信息"其实已落盘在别的目录，只是没被读；
2. **启动 `/research`（模式 B 深度叙事）或直接 `subagent`** 做定向补充调研；新调研内容**必须落盘到**：
   `<workspace>/Interview_Research/Complementary_Research/<专题名>/深度调研报告.md` + `sources/`（逐字摘录 + `SOURCES.md` 清单）；
3. **如实标注"未取得"**，并说明已检索范围与建议的函询渠道。

### Step 3：并行专题子代理
每专题 1 个后台 `subagent`，brief 必须**自包含**（子代理看不到主对话），并强制：
- 先穷尽工作区既有语料，再做增量检索；
- 每个采用的源落盘 + 建 `SOURCES.md`（本地文件名｜标题｜机构/年份｜URL｜一句话相关性｜LOCAL-EXISTING/NEW-WEB）；
- 每条事实带来源文件名；推算标 `【推算】`；矛盾标 `【冲突】`；未取得标 `【未取得】`；
- 返回**结构化要点**，不写最终报告（最终成文由主控负责，保证文风一致）。

> 经验：同时启动 3–5 个专题子代理效率最高；子代理容易超字数目标与堆砌细节，brief 里要写明"返回要点而非全文"。

### Step 4：主控叙事成文（Markdown）
按模板章节逐节写作，分文件管理便于增量编辑：
```
<输出目录>/.parts/
  part1.md   # 标题 + 第1–3章
  part2.md   # 第4章
  part3.md   # 第5–7章 + 附录（含参考文献 R# 条目）
```
写作纪律（逐条对应 R 要求）：
- **结论先行**；每个重要判断预留引用位（R5）；
- 数据带**单位与口径**，关键数字可回答"谁、被问了什么"（R6.2）；
- 推算写全**公式 + 参数 + 基准来源**，用 `【推算】` 标记并由转换器高亮（R6.1）；
- 数字越**量级上下限**必须解释或拆分；峰值类数字注明"非任一时点负载"（R6.3）；
- 任何主体的自述能力做**支持证据 / 不支持证据 / 偏离判定**三栏对照（R6.4）；
- 每条建议回指其**问题章节**并给出**量化代价**（R8）。

### Step 5：插入引用角标 + 生成参考文献
1. 先写好**附录《参考文献》**，条目形如 `R12. 机构《文件名》（文号/日期）。落盘：<路径>。`
2. 编写 `citations.json`：`section_refs`（章节号 → 引用编号）＋ `keyword_refs`（关键词正则 → 引用编号）。
3. 运行：
```bash
python3 assets/insert_citations.py --config citations.json part1.md part2.md part3.md
# 重跑前先清空： --strip
```
4. **人工补漏**：脚本会跳过引用块（`>`）与纯文字段落，需手工给"章末小结/引语/定位判断"等补上 `[[n]]`。
   ⚠️ 务必写**双中括号** `[[n]]`；写成单括号 `[n]` 只是普通文本，不会变成可点击角标。

### Step 6：Markdown → Word
```bash
cat <主md去掉首行标题> .parts/part2.md .parts/part3.md > .parts/full_draft.md
python3 assets/md2docx.py .parts/full_draft.md "<输出目录>/<报告名>.docx" \
        --title "<报告标题>" --toc
```
转换器已内置：模板样式基线、**字面量列表编号（无自动编号）**、`[[n]]` → 上标内部超链接、`Rn.` → 书签 + `[n]` 条目、目录、页脚页码、A4 页面。

### Step 7：验收自检（必须全绿）
```bash
python3 - <<'PY'
import re, zipfile
from docx import Document
from docx.oxml.ns import qn
f = "<报告>.docx"
d = Document(f); x = zipfile.ZipFile(f).read('word/document.xml').decode('utf-8')
print('styles:', set(p.style.name for p in d.paragraphs))          # 只应有 Normal
print('numPr:', len(re.findall(r'<w:numPr>', x)))                  # 必须 0
print('List styles:', len(re.findall(r'ListNumber|ListBullet', x)))# 必须 0
print('superscript:', len(re.findall(r'w:vertAlign w:val="superscript"', x)))
names = set(re.findall(r'<w:bookmarkStart [^>]*w:name="([^"]+)"', x))
anchors = set(re.findall(r'<w:hyperlink w:anchor="([^"]+)"', x))
print('unresolved anchors:', [a for a in anchors if a not in names])  # 必须 []

# ★ 多编号引用：必须逐编号成链
for p in d.paragraphs:
    m = re.search(r'\[(\d+(?:,\d+)+)\]', p.text)
    if m:
        exp = m.group(1).split(',')
        got = [h.get(qn('w:anchor')) for h in p._element.findall(qn('w:hyperlink'))]
        ok = got[:len(exp)] == ['_Ref%s' % n for n in exp]
        print('多编号样例', m.group(0), '->', got, 'OK' if ok else 'FAIL')
        break

# ★ 导航窗格标题树：统计 outlineLvl 分布（1=章 2=节 3=子节/附录分组）
lv = {}
for p in d.paragraphs:
    pPr = p._element.find(qn('w:pPr'))
    ol = pPr.find(qn('w:outlineLvl')) if pPr is not None else None
    if ol is not None:
        k = ol.get(qn('w:val')); lv[k] = lv.get(k, 0) + 1
print('outlineLvl:', lv)          # 附录分组应计入 3

# ★ 表格数据引用：统计含引用链接的单元格数与表格数
cells = sum(1 for t in d.tables for r in t.rows for c in r.cells
            if 'w:anchor="_Ref' in c._tc.xml)
print('tables:', len(d.tables), '| cells with citation:', cells)   # 重要表应逐行有

# ★ 僵尸文献：被引用编号集合必须覆盖 1..N 无缺口
nums = sorted({int(m.group(1)) for a in anchors if (m := re.match(r'_Ref(\d+)$', a))})
N = max(nums) if nums else 0
print('cited refs:', len(nums), '| 未被引用的条目:',
      [n for n in range(1, N + 1) if n not in nums])              # 必须 []

# ★ R6.1 推算三件套：标了【推算】却没有公式/参数的段落
bad = []
for p in d.paragraphs:
    t = p.text
    if '【推算】' in t and not re.search(r'[=≈×/*]', t):
        bad.append(t[:60])
print('【推算】缺公式/参数的段落:', len(bad), bad[:3])              # 期望 0
print('【推算】总数:', sum('【推算】' in p.text for p in d.paragraphs))

# ★ R6.3 护栏线索：含"算术和/峰值/合计"且同段有量级数的结论段，人工复核
peak = [p.text[:60] for p in d.paragraphs
        if re.search(r'算术和|峰值相加|相加得出', p.text)]
print('需人工复核"算术和当瞬时负载"的段落:', len(peak), peak[:3])

# ★ R8 问题—建议可追溯：附录是否存在映射表
keys = lambda t: {c.text.strip() for c in t.rows[0].cells}
print('问题—建议映射表存在:', any(
    ({'短板', '问题', '现状问题'} & keys(t)) and ({'建议', '方案', '对策'} & keys(t))
    for t in d.tables))                                            # 期望 True

# ★ R9-5 报告内必须有「当前算力分项汇总表」：含高校行 + 口径标注
hit = None
for t2 in d.tables:
    txt = ' '.join(c.text for r in t2.rows for c in r.cells)
    if ('高校' in txt or '大学' in txt) and ('装机' in txt or '在用' in txt) \
            and ('企业' in txt):
        hit = t2
print('当前算力分项汇总表存在:', hit is not None)                       # 期望 True

# ★ R9-1/2/4 由 parse_survey.py 的台账机器校验（必答题作答率 / 0 值单列 / q4↔q6 比值）
import os
led = os.path.join(os.path.dirname(os.path.abspath(f)), 'survey_current_compute.md')
print('当前算力台账存在:', os.path.exists(led), led)                    # 期望 True
if os.path.exists(led):
    s = open(led, encoding='utf-8').read()
    print('  必答题作答率 100%:', '100%' in s)
    print('  0 值单列:', '0＝无在用算力' in s or '0（0＝无' in s)
    print('  存在越界/未识别条目（需人工处置）:', s.count('⚠️') > 0)

# ★ R10 算力指标"逐值"精确引用：数值单元格必须自带角标（表头行不计）
MET = re.compile(r'\d[\d,\.]*\s*[/／]?\s*(?:PFLOPS|PFlops|P@FP16|Eops|EFLOPS|卡时|张|卡|%|GWh|MW)')
tt = ss = 0
for t2 in d.tables:
    for r2 in t2.rows[1:]:                      # 跳过表头行
        for c2 in r2.cells:
            if MET.search(c2.text):
                tt += 1
                if 'w:anchor="_Ref' in c2._tc.xml:
                    ss += 1
print('R10 数值单元格自带角标: %d/%d = %.0f%%' % (ss, tt, 100.0 * ss / max(tt, 1)))  # 目标 100%

# ★ R10-2 引用条目是否"引到条"（含日期/文号/URL/记录级定位之一）
# 注意：转换器把 `Rn.` 条目渲染成 `[n] …`，所以按 `^\[\d+\]` 匹配，不是 `^Rn\.`
weak = []
for p2 in d.paragraphs:
    if re.match(r'^\[\d+\]\s', p2.text.strip()):
        s2 = p2.text
        if not re.search(r'20\d\d|第\s*\d+\s*[条頁页]|URL|https?://|记录\s*#'
                          r'|落盘|\.md|\.csv|\.json|\.pdf|/', s2):
            weak.append(s2[:50])
print('R10 定位信息偏弱的参考文献条目:', len(weak), weak[:3])          # 期望 0

# 关键判断段落引用覆盖率
key = re.compile(r'结论|判断|建议|定位|总体架构')
tot = wit = 0
for p in d.paragraphs:
    t = p.text.strip()
    if len(t) < 25 or t.startswith('['): continue
    if key.search(t):
        tot += 1
        if p._element.findall(qn('w:hyperlink')): wit += 1
print('key paragraphs cited: %d/%d' % (wit, tot))                  # 目标 100%
PY
```
再打开 Word 抽查：列表编号是否每段独立、角标是否能点击跳转。

### Step 8：交付
`present` 主 `.docx`（＋ Markdown 源文件、补充调研专题报告），并在回复中说明：结构对应关系、引用体系、以及**未取得项/口径矛盾**。

---

## 三、资源文件

| 文件 | 用途 |
|---|---|
| `assets/md2docx.py` | Markdown → docx 转换器：模板样式、**无自动编号列表**、`[[n]]` 上标内部超链接、`Rn.` 书签、目录、页脚页码 |
| `assets/insert_citations.py` | 批量插入 `[[n]]` 角标（章节默认集 + 关键词触发；支持 `--strip` 重跑） |
| `assets/add_table_citations.py` | 按"(文件, 表头行号) → 各行引用"规则，批量为**表格数据行**追加 `[[n]]`（只改行尾，可重复执行） |
| `assets/parse_survey.py` | 把问卷 `responses.csv` + `survey_definition.json` 落成《当前算力台账》：必答题作答率、q4↔q6 硬件交叉校验、本地装机/在用两口径汇总（R9） |
| `assets/citations.example.json` | 引用配置示例（真实项目规模：86 条参考文献、332 个角标） |
| `TEMPLATE_SPEC.md` | docx 模板解析与样式基线对照表 |
| `CHECKLIST.md` | 交付前逐项验收清单 |

---

## 四、常见失败模式（真实踩过的坑）

| 失败现象 | 根因 | 规避 |
|---|---|---|
| **Word 打开后全文档跨标题连续编号** | 用了 `List Number`/`List Bullet` 样式或 `<w:numPr>` | 见 R2：字面量编号 + 普通段落 |
| **`[1,2,5]` 点哪个数字都只跳到 [1]** | 整串被当成**一个** hyperlink | 用 `add_citation()`：逐编号建链，`,` `[` `]` 为普通上标 |
| **附录分组标签不在导航窗格标题树里** | 用 `**加粗正文**` 冒充分组标题 | 写成 `#### 【一】…`（→ outlineLvl 3），并让其父级为 `### 附录X` |
| 群组标题出现在正文但不在目录 | 目录只收录 `##`/`###` | 目录生成按 `#{2,4}` 收录三级；或在 SKILL 配置中调整 |
| 角标只是普通文本，点击无反应 | 写了单括号 `[n]` 或未建书签 | 写 `[[n]]`；条目用 `Rn.` 前缀 |
| 点击角标跳转失败/报错 | anchor 与书签不匹配，或书签 id 重复 | Step 7 校验 `unresolved anchors == []`，且链接数=编号个数之和 |
| 表格里的数据没有来源 | 转换器对表格 `allow_cite=False`，且底稿未在单元格内写 `[[n]]` | 转换器改用 `allow_cite=True, cite_size=8.0`；用 `assets/add_table_citations.py` 按"行→引用"规则批量追加 |
| 参考文献表里有从未被引用的条目（僵尸文献） | 只检查"段落是否有角标"，没检查"条目是否被引用" | Step 7 增加"被引用编号集合 vs 1..N"的差集校验，输出必须为空 |
| 推算只标了 `【推算】` 却给不出公式和参数 | 把"打标签"当成"讲清楚"，公式留在底稿/子代理报告里 | R6.1 三件套：正文写公式 + 参数取值 + 基准来源；Step 7 断言"标了标记但无公式的段落 = 0" |
| 报告里的数字来源说不清 | 只写"口径"没写"采集通道" | R6.2 三问（量纲/采集通道/口径年份）+ 抽查 10 个数字能否定位到 `sources/` |
| 总算力需求被系统性高估 | 把 N 个场景/单位的**峰值算术和**当成瞬时负载 | R6.3 量级护栏：峰值类数字注明"非任一时点负载"，并剥离客户侧/非自有资产 |
| 把企业自述当成结论 | `company` 级证据单独支撑判断，未做交叉验证 | R6.4 三栏对照（支持证据/不支持证据/偏离判定），偏离须给可核验反证 |
| **把必答题的答案记成"无回复/未公开"** | 只看"空单元格"，没看 `survey_definition.json` 的 `required` 标记 | R9-1：先算完整提交的作答率；必答题 100% 有值即不得写"无回复" |
| **把 `0` 当成缺答** | `0`（无在用算力）与空值未分开统计 | R9-2：`0` 单列为有效值，报告写"自报 0（无在用算力）" |
| 未做校验就整体否定自报值 | 用"单位存疑/可能按卡数理解"一句话打掉整批数据 | R9-4：先做 q4↔q6 逐条换算比对，0.5—2.0× 即判自洽采用 |
| 当前算力漏掉高校侧 | 只统计问卷（企业）样本，把高校装机当成"另一章的事" | R9-5：总口径＝企业＋高校＋政务/社会资本，分项列示并注明口径 |
| 本地装机与"在用算力（含云租）"混算 | 两个口径相加当成一个数 | R9-3：两口径分列；混合型企业给下界（纯自建）与上界（含混合）区间 |
| **算力数字的角标"粗到行"** | 角标统一加在表格最后一列（来源/备注列），数值本身没有角标 | R10-1：角标贴到**数值所在单元格**；Step 7 测"数值单元格自带角标率＝100%" |
| 引用条目只说"某调研报告"，无法定位 | 组引用兜底（`[[74,75,77]]`），条目缺 URL/日期/页码/记录号 | R10-2：按来源类型补"文号/URL+抓取日期/页码/记录级定位"；Step 7 扫描定位信息偏弱条目 |
| 推算的算力值只引"本报告推算" | 入参来源未逐项引用 | R10-3：逐入参引用来源，与 R6.1 三件套配套 |
| 两个口径的算力值共用一个角标 | 如 `4 Eops` 与 `1.16 EFLOPS` 合并引用 | R10-5：各自引用并注明口径 |
| 建议很漂亮，但对应不上任何现状问题 | 建议章与现状章各写各的，无锚点 | R8：建议回指问题章节 + 量化代价 + 附录映射表 |
| 推算里出现 LaTeX 公式（`$...$`）被原样输出 | md2docx 不支持 LaTeX/MathML | 公式一律写**纯文本**（`P ≈ QPS × T × M / η`）；确需排版公式时改用图片并说明 |
| 结论段落漏引用 | 自动脚本跳过 `>` 引用块与无数字段落 | Step 5 第 4 条：人工补漏 + Step 7 覆盖率检查 |
| 参考文献被正文引用符号污染 | 参考文献行也参与打标 | 条目用 `Rn.` 前缀，脚本对附录区整体跳过 |
| 大文件反复用 `edit` 极慢且易错 | 逐处编辑 40k+ 字文档 | 用 Python 脚本批量替换/插入；分 part 文件增量维护 |
| 子代理产出与主报告文风割裂/超长 | 让子代理直接写"成稿" | 子代理只产**事实底稿 + 落盘源**，成稿由主控统一写 |
| 数字自相矛盾 | 不同来源口径不同却静默择一 | R6：并录两个口径并注明差异 |
| 简体/繁体、公司名混乱 | 直接沿用来源用字 | R7：正文转简体，专名保留登记原文 |
